#!/bin/bash

# Can use templates

echo "Installing xcp and xpb shell"

{{ $xcp := joinPath .chezmoi.sourceDir "tools" "scripts" "xcp" | quote }}
{{ $xpb := joinPath .chezmoi.sourceDir "tools" "scripts" "xpb" | quote }}

# copy with permission
sudo cp -p {{ $xcp }} {{ $xpb }} /usr/bin